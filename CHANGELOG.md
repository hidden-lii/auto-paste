# 更新日志

本项目的所有重要变更均记录于此文件。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.4] - 2026-07-27

### 新增

- 账号支持多角色区服（独立 `role` 表），区服从 JX3API 同步入库，表单内可搜索选择
- 区服选择支持模糊搜索（区服名 / 大区 / 状态），列表按状态（爆满 → 拥挤 → 正常）及拼音首字母排序
- 卡片右上角「更多」菜单：修改、删除、分享
- 分享支持按导出设置复制格式化文本；导出设置独立 Dialog
- 首次启动网络授权弹窗；功能面板可开关联网同步区服
- 底部收藏筛选改为向上展开 radio 面板（全部 / 收藏 / 未收藏）

### 变更

- 收藏文案统一为「已收藏」/「未收藏」
- 功能面板用户名/密码切换改为 radio 样式

---

## [1.1.3] - 2026-07-22

### 新增

- 功能面板支持设置「默认隐藏用户名 / 默认隐藏密码」，偏好会持久化到数据库并在启动时自动恢复
- 新增应用版本查询接口（`get_app_version`、`get_current_app_version`）
- 新增 `CHANGELOG.md` 记录版本变更

### 变更

- 账号与分组优先级输入由下拉选择改为数字输入框，支持任意正整数
- 拖拽排序改为通过专用拖动手柄触发，避免与卡片点击操作冲突
- 账号列表隐藏滚动条，拖拽排序交互与视觉反馈优化
- 数据库启动时自动检查版本并执行合并迁移，兼容旧版 `priority` 字段数据
- 查询语句显式列出字段，避免表结构变更后出现列名冲突

### 修复

- 移除误添加的 npm `cargo` 依赖
- 停止追踪本地数据库文件 `auto_paste.db`

---

## [1.1.2] - 2026-07-22

### 新增

- **账号拖拽排序**：在「全部」分组且无搜索/筛选时，支持拖拽调整账号显示顺序，排序自动保存
- **窗口尺寸记忆**：自定义窗口大小并持久化到数据库，启动时自动恢复；支持一键恢复默认尺寸
- **窗口置顶**：底部工具栏新增置顶按钮，可将窗口固定在最前
- **用户名/密码脱敏**：默认对用户名中间段和密码进行掩码显示，可在功能面板中切换明文展示
- **功能面板**：左下角新增功能菜单，集中管理脱敏开关与窗口大小设置
- **分组标签右键菜单**：在分组标签上右键可快速修改或删除分组

### 改进

- **架构重构**：将 `AccountPage` 拆分为独立组件（账号卡片、搜索栏、分组标签、表单对话框等）与 API 层，提升可维护性
- **交互优化**：账号卡片支持展开查看备注；编辑/新增时增加未保存退出确认
- **收藏筛选**：底部工具栏支持循环切换「全部 / 仅收藏 / 仅未收藏」
- **窗口启动**：应用启动时先应用已保存的窗口尺寸再显示，避免闪烁
- **数据迁移**：旧版 `localStorage` 中的窗口尺寸自动迁移至 SQLite；兼容物理像素与逻辑像素的尺寸数据

### 修复

- 修复引用传递导致的属性不正确变更问题
- 修复 `package-lock.json` 使用公共 npm 源，确保 CI 可正常安装依赖

### 文档

- 完善 README，补充功能说明、技术栈、项目结构、数据存储与使用指南

---

## [1.1.1] - 2023

### 新增

- GitHub Actions 支持 macOS 与 Windows 自动构建

### 改进

- UI 布局与样式调整
- 修复部分已知问题

---

## [1.1.0] - 2023

### 新增

- **分组（Category）功能**：支持创建、修改、删除分组，账号与分组多对多关联
- 分组标签页展示与切换筛选
- 分组内账号关联管理

### 改进

- 将 `group` 统一重命名为 `category`
- 优化部分代码结构与界面显示

---

## [1.0.0] - 2023

### 新增

- 基于 Tauri + Vue 3 + Vuetify 的桌面端账号管理应用
- 账号增删改查与一键复制用户名/密码到剪贴板
- 账号收藏标记
- 按名称/账号关键词搜索
- 本地 SQLite 数据存储
- macOS 独立配置文件
- 界面汉化与操作提示对话框
- MIT 开源许可证

[1.1.4]: https://github.com/hidden-lii/auto-paste/compare/v1.1.3...v1.1.4
[1.1.3]: https://github.com/hidden-lii/auto-paste/compare/v1.1.2...v1.1.3
[1.1.2]: https://github.com/hidden-lii/auto-paste/compare/v1.1.1...v1.1.2
[1.1.1]: https://github.com/hidden-lii/auto-paste/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/hidden-lii/auto-paste/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/hidden-lii/auto-paste/releases/tag/v1.0.0
